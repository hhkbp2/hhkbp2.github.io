<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Lisp | HHKBP2]]></title>
  <link href="http://hhkbp2.github.com/blog/categories/lisp/atom.xml" rel="self"/>
  <link href="http://hhkbp2.github.com/"/>
  <updated>2019-06-21T19:41:33+08:00</updated>
  <id>http://hhkbp2.github.com/</id>
  <author>
    <name><![CDATA[Dylan.Wen]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Kapok 的设计与实现: Protocol]]></title>
    <link href="http://hhkbp2.github.com/blog/2018/03/19/kapok-the-design-and-implementation-of-protocol/"/>
    <updated>2018-03-19T14:56:00+08:00</updated>
    <id>http://hhkbp2.github.com/blog/2018/03/19/kapok-the-design-and-implementation-of-protocol</id>
    <content type="html"><![CDATA[<p><a href="https://github.com/kapok-lang/kapok">Kapok</a> 是我设计与实现的一个基于 Erlang VM 的现代 Lisp 编程语言。<a href="/blog/2016/06/08/minds-echo/">《念念不忘，亦有回响》</a>这篇文章叙述了这门编程语言的概况。下面我们来聊一聊其中 Protocol 的具体设计和实现。</p>

<h2 id="protocol-">Protocol 是什么</h2>

<p>为了更好地进行程序的编写，编程语言往往需要引入一些抽象，比如一个函数是一系列操作的封装，一个模块是一堆函数的封装，然后出于通用性的需要，又定义了函数签名来区分具有相同参数类型和参数个数的函数，对于具有相同函数签名的模块，在 Erlang 我们定义了行为(Behavior)。类似地，Protocol 也是一种对数据类型和基于数据类型上实现的一些函数的抽象。在面向对象编程语言(OOP)非常流行的今天，这种抽象更多地被称为类与接口。</p>

<p>下面简单举例来说明 Protocol 是什么。在 Erlang 里面，如果我们要编写一段代码来处理不同的数据类型，往往需要在一大块集中代码里面针对每一种数据类型进行不同的处理。假设我们正在编写一个 Json 模块，这个模块要处理 list, binary, number 三种类型的 encode(编码)操作，那么就有代码：</p>

<p>```erlang
-module(json)</p>

<p>encode(Item) when is_list(Item) -&gt;
  % …
encode(Item) when is_binary(Item) -&gt;
  % …
encode(Item) when is_number(Item) -&gt;
  % …
```</p>

<p>如果要添加更多的数据类型，那么就在模块内部添加对应那个数据类型的分支。但是如果你没有这个模块的源代码就无法添加，而且经过长时间添加多个类型支持后这个模块变得非常大，难于维护。Elixir 引入了 Protocol，针对上述的 encode 操作，可以定义成一个 Protocol，代码如下：</p>

<p><code>elixir
defprotocol JSON do
  def encode(item)
end
</code></p>

<p>对于任何实现了<code>JSON</code> Protocol 的数据类型对象<code>data</code>，都可以直接调用下述函数：</p>

<p><code>elixir
JSON.encode(data)
</code></p>

<p>这些数据类型的<code>JSON</code> Protocol 实现可以分散放在各个文件中，没有要集中维护的问题，当你需要添加一个新的数据类型的支持时，也不需要已有模块的源代码。</p>

<p>```elixir
defimpl JSON, for: List do
  def encode(item) # …
end</p>

<p>defimpl JSON, for: BitString do
  def encode(item) # …
end</p>

<p>defimpl JSON, for: Number do
  def encode(item) # …
end
```</p>

<p>通过 Protocol 这样一个抽象，我们可以在函数式的编程语言中，定义对数据类型绑定一系列的接口，然后针对这些通用的接口来进行编程。针对接口编程一个常常被提到的用法是，在需要快速编写原型的时候，使用简单的数据类型进行接口编程，先快速实现功能，等到程序稳定下来，后期需要进行性能优化的时候，再将接口下面的数据类型更换成更高效的实现，这个过程中所有的接口使用代码都不需要修改。</p>

<h2 id="elixir--protocol-">Elixir 中 Protocol 的实现</h2>

<p>Protocol，或者说类接口，它的广义的概念可以追溯到 20 世纪 80 年代开始流行的面向对象编程语言，比如 C++，甚至更早之前 60~70 年代就已经存在的 Lisp，从那个年代流传至今的很多 Lisp 方言中，都或多或少有着这种抽象，比如 Common Lisp 中的 Sequence 的概念。而在 Erlang VM 上实现了 Protocol 的 Elixir 语言，它则是参考了 Clojure 的 Protocol 定义和实现。下面简单描述一下 Elixir 的 Protocol 实现。</p>

<p>从上面的例子我们可以看到，Protocol 的本质是将数据类型和接口分开定义，并在运行时进行动态绑定。上述例子中，对于 List 类型，定义了对应的 encode 实现，调用 Protocol 的接口<code>JSON.encode(data)</code>时，如果 data 是 List 类型，就进行动态分发，执行对应的 encode 实现。那么如何实现这种动态绑定或者说分发呢？由于 Erlang 是一个函数式编程语言，Erlang VM 的基本语义中也只支持模块和函数，因为 Protocol 的每个实现可以分开，而 Erlang VM 中基础的编译单元是模块，一个简单的映射方法就是将每个 Protocol 实现映射为一个模块。而对于接口模块<code>JSON</code>，很自然地也成为一个单独的模块。那么就只剩下一个问题了，即如何实现当调用<code>JSON.encode()</code>时，将执行路径转到<code>defimpl JSON, for: List</code>模块的<code>encode()</code>函数，这里可以实现为在运行时拼接模块名，然后调用具体模块的函数。上述的 Elixir 代码中的例子，将编译成对应的 Erlang 代码：</p>

<p><code>
          Elixir                                  Erlang
defprotocol JSON do                     -module(JSON).
  def encode(item)         生成          encode(item) -&gt;
end                                        case item of
                                             data when is_list(data) -&gt;
                                               'JSON.List':encode(data);
                                             data when is_binary(data) -&gt;
                                               'JSON.BitString':encode(data);
                                             data when is_number(data) -&gt;
                                               'JSON.Number':encode(data)
                                             data when is_struct_X(data) -&gt;
                                               'JSON.X': encode(data)
                                           ...
</code></p>

<p>这里的 struct_X 表示除了几个基本数据类型以外的用户自定义 struct 类型，其中 X 为 struct 的名字，即对应 Elixir 中的代码</p>

<p><code>
defmodule X do
  defstruct # ...
end
</code></p>

<p>对于各个数据的类型的实现，可以映射为各个 Erlang 模块：</p>

<p>```
          Elixir                                         Erlang
defimpl JSON, for: List do            生成      -module(‘JSON.List’).
  def encode(item) # …                        encode(item) -&gt;
end                                               %% …</p>

<p>defimpl JSON, for: BitString do                 -module(‘JSON.BitString’).
  def encode(item) # …                        encode(item) -&gt;
end                                               %% …</p>

<p>defimpl JSON, for: Number do                    -module(‘JSON.Number’).
  def encode(item) # …                        encode(item) -&gt;
end                                               %% …
```</p>

<p>注意其中的模块名是由 Protocol 名字，即这个例子中的 JSON，和具体的数据类型名，即这个例子中的 List, BitString, Number 等，两者拼接而成。</p>

<h2 id="kapok--protocol-">Kapok 中 Protocol 的实现</h2>

<p>既然 Elixir 已经实现了 Protocol，而且 Kapok 和 Elixir 都兼容于 Erlang VM，那么除了另外捣腾一套 Protocol 的用法和实现之外，比较好的做法就是兼容 Elixir 的实现，从而达到重用所有 Elixir 己经有的库和代码的效果。Elixir 中定义了每个 Protocol 模块都必需具备的接口函数，具体列出如下，它们都是通过 defprocotol 宏来实现的：</p>

<ul>
  <li>
    <p><code>__protocol__/1</code> 
当参数是<code>:name</code>，返回 Protocol 名字
当参数是<code>:functions</code>，返回一个元素为 Protocol 接口函数和参数个数的关键字列表
当参数是<code>:impls</code>，返回一个实现的列表</p>
  </li>
  <li>
    <p><code>impl_for/1</code>
接收一个 struct，返回为此 struct 实现当前 Protocol 的模块名，如果不存在这样的实现模块则返回<code>:nil</code></p>
  </li>
  <li>
    <p><code>impl_for!/1</code>
类似于上面的<code>impl_for/1</code>，区别在于当实现模块不存在时不返回<code>:nil</code>而是抛出异常</p>
  </li>
</ul>

<p>在 Kapok 中，也定义类似的 Macro<code>defprotocol</code>来生成这些接口函数。类似地，对于 Elixir 中的<code>defstruct</code>, <code>defimpl</code> Macro，Kapok 也定义了对应的 Macro 来生成对应的 struct 结构，和实现函数接口。</p>

<p>以上所述的种种 Protocol 结构都是通过 Macro，这个强大的 Lisp 编程方式，来实现的。通过 Macro 处理手写代码，生成新代码，我们可以在 Kapok 实现一套类似于 Elixir Protocol 原语，无缝对接到已有的 Elixir 代码和库中。</p>

<h2 id="kapok--protocol--1">Kapok 的 Protocol 使用示例</h2>

<p>最后附上一个 Kapok 代码示例，展示一下 Protocol 在 Kapok 中的定义和使用，便于理解。</p>

<p>```clojure
;; 定义一个用于此示例的 Protocol
(defprotocol pr
  “A protocol to print something.”
  ;; 此 Protocol 唯一接口用来打印某个数据
  (print [self]))</p>

<p>;; 为 Atom 数据类型实现 pr Protocol
(defimpl pr Atom
  (require io)</p>

<p>(defn print [atom]
    (io.format “print an atom: #’~p’~n” [atom])))</p>

<p>;; 为 List 数据类型实现 pr Protocol
(defimpl pr List
  ;; 在 print 的实现代码中，直接使用了 Elixir 的 Enum Protocol
  ;; 所以这里用了一行代码引入 Elixir.Enum 模块
  (require (Elixir.Enum :as enum)
           io)</p>

<p>(defn print [list]
    (io.format “print a char list: #"”)
    ;; 直接在 List 上调用 enum.map，直接使用 Elixir 库代码
    (enum.map list
              (fn [x] (io.format “~c” [x])))
    (io.format “"~n”)))</p>

<p>(ns protocol-examples
  (require pr))</p>

<p>(defn main []
  ;; 下面先后声明了 Atom 和 List 两种数据类型的实例，并调用 pr.print 接口
  (let [data #abc]
    (pr.print data))
  (let [data #”abc”]
    (pr.print data)))
;; 得到输出
print an atom: #’abc’
print a char list: #”abc”
```</p>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Kapok 的设计与实现: Macro]]></title>
    <link href="http://hhkbp2.github.com/blog/2017/08/14/kapok-the-design-and-implementation-of-macro/"/>
    <updated>2017-08-14T16:20:00+08:00</updated>
    <id>http://hhkbp2.github.com/blog/2017/08/14/kapok-the-design-and-implementation-of-macro</id>
    <content type="html"><![CDATA[<p><img src="/images/uploads/kapok_flowers.jpg" title="“” “木棉花”" ></p>

<p><a href="https://github.com/kapok-lang/kapok">Kapok</a> 是我设计与实现的一个基于 Erlang VM 的现代 Lisp 编程语言。<a href="/blog/2016/06/08/minds-echo/">《念念不忘，亦有回响》</a>这篇文章叙述了这门编程语言的概况。下面我们来聊一聊其中 Macro 的具体设计和实现。</p>

<p>Lisp 是一系列采用括号包围中缀表达式的列表形式来编写代码的编程语言的统称，借用一位朋友的说法，Lisp 就是“裸写 AST”。由于这种特别的写法，在 Lisp 里面将代码作为数据来操作相当容易，而实现这种操作的就是 Macro。有很多的书籍和资料都有描述 Lisp Macro，这里就不展开了，简单的来说，Lisp Macro 是一种在编译时执行的，接受列表输入，输出列表的特殊函数。如果我们构思一下如何在 Erlang VM 上为一门 Lisp 语言实现 Macro，可以得到这几个要点：</p>

<ol>
  <li>Macro 实现为函数，而 Erlang 和 Erlang VM 支持函数  </li>
  <li>Macro 对应的实现函数，其输入是代码 AST，输出也是 AST  </li>
  <li>Macro 在编译时执行，Macro 本身的代码需要在被调用前的代码编译前编译完成  </li>
</ol>

<p>下面我们进一步看看 Kapok Macro 的具体实现。</p>

<h2 id="section">函数的实现</h2>

<p>将 Lisp Macro 实现为函数，首先我们要在 Erlang VM 上实现 Lisp 函数。Erlang 是一门函数式编程语言，在它的代码编译过程中，生成的 Abstract Form(或称为 Syntax Tree)和 Core Erlang 这两种中间代码都直接支持函数。Erlang 函数比较特别，参数个数(Arity)也是函数签名的一部分，它支持多个子句但是每个子句只能有固定个数的参数，举例<code>erlang:spawn/1</code>和<code>erlang:spawn/3</code>是一个同名函数的两个子句。目前已经存在的几个 Erlang VM 上的语言实现，如 LFE 等，大都沿用了 Erlang 函数的这个设计，除了 Elixir 在函数参数的最后面引入 Keyword List。如果将 Kapok 的函数直接编译成 Erlang 函数，函数参数定义和签名可以照抄过来，是容易做到的。但传统的 Lisp，比如 Common Lisp, 在函数参数定义这块支持<code>&amp;optional</code>, <code>&amp;rest</code>, <code>&amp;key</code>这几个关键字，其中<code>&amp;optional</code>表示这个关键字后面的参数是可选的，<code>&amp;rest</code>表示将后面的参数构造成一个 list，<code>&amp;key</code>表示可以用 key value 匹配的形式来调用函数。能不能在 Erlang VM 上实现对这几个关键字的支持呢？不妨尝试一下。</p>

<p>首先思考一下如何实现<code>&amp;optional</code>，我们知道<code>&amp;optional</code>后面的参数在调用时可以不传入而采用默认值。举例来说，如果在 Common Lisp 里面有这个函数定义</p>

<p><code>
(defun f (a &amp;optional b c)
  ;; 具体实现
  ...
  )
</code></p>

<p>以下面几种方式来调用都是合法的</p>

<p><code>
(f 1 2 3)
(f 1 2)
(f 1)
</code></p>

<p>这种调用的形式可以适配到 Erlang 的子句上面来。比如上面的函数定义<code>f</code>可以实现成 Erlang 函数：</p>

<p><code>
f(A) -&gt;
  f(A, nil, nil).
f(A, B) -&gt;
  f(A, B, nil).
f(A, B, C) -&gt;
  %% 具体实现
  ...;
</code></p>

<p>如果参数 B, C 被省略了，那么就用一个默认值来调用参数多一个的子句，这里假定默认参数统一为 Atom nil(Lisp 里面 optional 参数的默认值为 symbol nil)。</p>

<p>然后再考虑如何实现<code>&amp;rest</code>, <code>&amp;key</code>。我们看看一个常用的函数参数实现技巧。假设现在需要定义一个函数，这个函数只允许有一个参数(不考虑这个限制是否合理)，然而你需要传入两个参数，你会怎么定义这个函数？</p>

<p><code>
%% An Erlang function which calculates the area of a rectangle
%% with specified length and width.
calc_area({length, width}) -&gt;
   length * width.
</code></p>

<p>你会将这个函数的唯一参数定义成一个能存放两个参数的结构，然后在调用时用实际参数构造一个这样的结构传入。上面是一个以 Erlang 编写的例子，事实上用其它语言比如 Python, C 来实现，思路也是一样。</p>

<p>类似地，对于<code>&amp;rest</code>参数，可以将<code>&amp;rest</code>后面的参数构造成一个列表传入。举例说明，若有 Lisp 的函数定义</p>

<p><code>
(defun f (a &amp;rest b)
  ;; 具体实现
  ...
  )
</code></p>

<p>可以翻译成对应的 Erlang 函数如下：</p>

<p><code>
f(A) -&gt;
  f(A, []).
f(A, B) -&gt;
  ;; 具体实现，此处 B 是一个列表
  ...
</code></p>

<p>如果调用 Lisp 函数<code>f</code>的实际参数超过一个，那么从第二个参数开始及之后的实际参数都会被放入一个 List 中，赋值给<code>B</code>变量。比如对于调用</p>

<p><code>
(f 1 2 3 4)
</code></p>

<p>那么实际调用的对应代码就变成</p>

<p><code>
f(A = 1, B = [2 3 4])
</code></p>

<p>对于关键字<code>&amp;key</code>，也可以采取类似的思路。比如对于函数定义</p>

<p><code>
(defun f (a &amp;key b c)
  ;; 具体实现 
  ...
  )
</code></p>

<p>会翻译成对应的 Erlang 函数定义</p>

<p><code>
f(A) -&gt;
  f(A, maps:new()).
f(A, Map) -&gt;
  %% 具体实现
  ...
</code></p>

<p>调用时会变换实际参数为第二个形参构造一个 Map。比如这样的调用</p>

<p><code>
(f 1 :c 3)
</code></p>

<p>会翻译成</p>

<p><code>
f(A = 1, Map = #{c =&gt; 3})
</code></p>

<p>所描述的都是 Lisp 函数定义里，固定参数后出现<code>&amp;optional</code>, <code>&amp;rest</code>, <code>&amp;key</code>三个关键字其中之一的简单情况。如果两个关键字都用上，还是否能够工作呢？我们知道<code>&amp;rest</code>, <code>&amp;key</code>都在最后一个参数上做文章，如果<code>&amp;rest</code>, <code>&amp;key</code>同时出现，那最后一个参数是构造成 List 还是 Map？幸好这种情况不会发生，在 Common Lisp 的编程规范中限制了函数定义中<code>&amp;optional</code>, <code>&amp;rest</code>, <code>&amp;key</code>这几个关键字的用法如下：</p>

<ol>
  <li>固定参数 + <code>&amp;optional</code>/<code>&amp;rest</code>/<code>&amp;key</code> 其中之一的简单情况，上面我们已经过了一遍</li>
  <li><code>&amp;key</code>与<code>&amp;optional</code>, <code>&amp;rest</code>混用会产生奇怪行为，所以<code>&amp;key</code>不能与它们混用</li>
  <li><code>&amp;optional</code>可与<code>&amp;rest</code>混用，但是约定<code>&amp;optional</code>在前，<code>&amp;rest</code>在后</li>
</ol>

<p>只有第 3 点是混用的情况，根据的实现方式可知<code>&amp;optional</code>和<code>&amp;rest</code>没有冲突，第 3 点是可以实现的。</p>

<p>上面花了很长的篇幅来详细描述如何在 Erlang VM 实现 Lisp 函数，由开头的讨论可知 Macro 也是函数，有了函数的实现作为基础，下面我们进一步讨论 Macro 函数的输入输出。</p>

<h2 id="ast-">AST 的处理</h2>

<p>从代码的书写形式来看，在 Lisp 里面代码是列表，因此 Lisp Macro 的输入和输出都是列表。在实现层面，Lisp 列表形式的代码会经过词法分析语法分析，最后变成编译器内部表示 AST。AST 主要保存这几类信息：类别，数值，元数据。例如，在 Lisp 代码里面的<code>100</code>字面量，就需要一个 AST 数据结构记录下它是一个常量，值为整数 100，它在代码文件里面的行号是多少等。Kapok 内部将 AST 定义为 Erlang Tuple</p>

<p><code>
{Category, Meta, Value}
</code></p>

<p>其中<code>Category</code>表示这个结点的类别，比如 integer 常量，list, map 等。<code>Meta</code>是一个保存元数据的 Property List。<code>Value</code>则是记录了实际的取值，比如对于 integer 常量，就记录了常量的值是 100，对于 List 类型，就记录了这个 List 包含的所有元素的子结点 AST。</p>

<p>熟悉 Elixir 元编程的人看到这个定义会觉得很熟悉，因为 AST 在 Elixir 中的定义也是如此。</p>

<p>Macro 的输入和输出都是 AST，在编译过程中，每一个对 Macro 的调用，都是使用参数的对应的 AST 来调用 Macro 的实现函数。举例说明，比如对于 Macro 定义</p>

<p><code>
(defmacro unless [test &amp;rest body]
  """Evaluates test. If logical false, evaluates body in an implicit do."""
  `(case (core.true? ~test)
     (^true ^ok)
     (^false ~@body)))
</code></p>

<p>有调用代码如下</p>

<p><code>
(unless (f 1)
  (g 2)
  (h 3))
</code></p>

<p>那么在 Macro 展开的时候会得到调用如下，以对应 Erlang 代码来描述：</p>

<p><code>
unless(AST{(f 1)}, [AST{(g 2)}, AST{(h 3)}])
</code></p>

<p>其中<code>AST{X}</code>表示 Lisp 表达式<code>X</code>(也就是在 Lisp 代码里面看到的形式)对应的 AST，每个 AST 展开都是上面所述的 Tuple 结构，为方便描述这里使用了简单表示。Macro 展开的过程，即是输入的 AST 流经过 unless 函数，对应的参数经过变换之后得到输出如下:</p>

<p><code>
AST{(case (core.true? (f 1))
      (^true ^ok)
      (^false (g 2) (h 3)))}
</code></p>

<p>整个输出是一个完整的 case 表达式的 AST，检查 case 表达式和 unless Macro 的定义，我们可知<code>AST{(f 1)}</code>插入到了指定的条件判断参数处，而<code>[AST{(g 2)}, AST{(h 3)}]</code>中的元素被取出放入了<code>false</code>分支。</p>

<p>在 Kapok 编译的最后阶段，这种内部 AST 会转换成 Erlang Abstract Form，最后把它用 Erlang Compiler 编译并加载到 Erlang VM。这部分的转换的逻辑相当于繁琐，篇幅所限就不再一一展开。</p>

<h2 id="macro-">Macro 编译，展开</h2>

<p>普通函数一般是在运行时执行的，Macro 与之不同，它在编译时执行。要在编译时调用一个 Macro，这个 Macro 的定义必须已知，并且经过编译，才可能被调用。在 Erlang 里面，模块(Module)即是代码的基本组织单元，也是编译的基本单元，虽然也可以对单个表达式进行编译。已有的几种基于 Erlang VM 的语言，如 Elixir, LFE，都采用了将他们的代码模块翻译到 Erlang 模块然后按模块编译的方式。按模块编译对于 Macro 的编译和展开会产生一个编译顺序的问题，举例来说在 Elixir 里面，你需要先声明一个模块<code>m1</code>，在里面编写宏<code>mac</code>的定义，然后声明另外一个模块<code>m2</code>，在<code>m2</code>里面的代码，假设是函数<code>f</code>，调用<code>mac</code>，编译的时候会先编译<code>m1</code>，使得<code>mac</code>的定义被编译过，然后编译<code>m2</code>，编译<code>m2</code>时在<code>mac</code>被调用的地方执行它。你不能把<code>f</code>和<code>mac</code>都写在同一个模块，否则在这个模块被编译的时候，<code>mac</code>就不能被执行。在 Kapok 中，我沿用了 Joxa 的一个技巧，<code>f</code>和<code>mac</code>可以写到同一个模块，但是<code>mac</code>必须放在<code>f</code>之前，在编译时，如果发现<code>f</code>中的代码调用了一个当前模块的 Macro，那么就将<code>f</code>之前的代码当成一个完整的模块，以一个随机生成的模块名，送到 Erlang Compiler 编译，编译后就可以调用其中的<code>mac</code>。</p>

<p>每次调用一个 Macro，都会将调用时的参数 AST 传入，得到结果 AST，再插入到原来调用处，这样称之为一次展开。在 Lisp 中，Macro 的展开是递归的，即是说如果展开<code>mac</code>之后，得到的代码若还有其它的 Macro，比如<code>unless</code>，还会进一步展开，直至所有 Macro 都全部展开为止。</p>

<h2 id="section-1">一个完整的例子</h2>

<p>最后，以一个完整的例子来说明整个 Kapok 编译过程，包括上面提到的函数翻译，Macro 的编译和展开等，以帮助理解。用一个简单的 Kapok 函数定义为例</p>

<p><code>
(defn g [n]
  (io.format "call g(n = ~p)~n", [n]))
</code></p>

<p>这段代码经过词法分析，语法分析阶段后，会生成 AST 如下：</p>

<p><code>
{list,[{line,4},{column,1}],
      [{identifier,[{line,4},{column,2}],defn},
       {identifier,[{line,4},{column,7}],g},
       {literal_list,[{line,4},{column,9}],
                     [{identifier,[{line,4},{column,10}],n}]},
       {list,[{line,5},{column,3}],
             [{dot,[{line,5},{column,6}],{io,format}},
              {binary_string,[{line,5},{column,14}],&lt;&lt;"call g(n = ~p)~n"&gt;&gt;},
              {literal_list,[{line,5},{column,34}],
                            [{identifier,[{line,5},{column,35}],n}]}]}]}
</code></p>

<p>其结构比较简明，从中可以识别出标识符<code>defn</code>, <code>g</code>, <code>n</code>及<code>list</code>等结构。这个 AST 由于没有任何 Macro 调用，经过 Macro Expand 阶段没有任何变化，然后会转换成 Erlang Abstract Form 如下：</p>

<p><code>
{function,0,g,1,
 [{clause,4,
   [{var,4,n}],
   [],
   [{call,5,
     {remote,5,{atom,0,io},{atom,0,format}},
     [{bin,0,[{bin_element,0,{string,0,"call g(n = ~p)~n"},default,default}]},
      {cons,0,{var,5,n},{nil,0}}]}]}]}
</code></p>

<p>这段中间代码同样比较简单，容易看到函数<code>g</code>只带一个参数<code>n</code>，在它唯一的一个子句里面调用了一个<code>io.format</code>函数。</p>

<p>我们可以加上前面提到的 Macro，并且添加一个调用 Macro 的代码，如下：</p>

<p>```
(defmacro unless [test &amp;rest body]
  “"”Evaluates test. If logical false, evaluates body in an implicit do.”””
  (io.format “call unless macro~n”)
  `(case (core.true? ~test)
     (^true ^ok)
     (^false
      (io.format “before exprs in body!~n”)
      ~@body)))</p>

<p>(defn f [n]
  (g n)
  (unless (== n 0)
    (io.format “1. n: ~p~n” [n])
    (io.format “2. hello macro~n”)))
```</p>

<p><code>f</code>的定义在经过词法与语法分析阶段后得到 AST</p>

<p><code>
{list,[{line,16},{column,1}],
      [{identifier,[{line,16},{column,2}],defn},
       {identifier,[{line,16},{column,7}],f},
       {literal_list,[{line,16},{column,9}],
                     [{identifier,[{line,16},{column,10}],n}]},
       {list,[{line,17},{column,3}],
             [{identifier,[{line,17},{column,4}],g},
              {identifier,[{line,17},{column,6}],n}]},
       {list,[{line,18},{column,3}],
             [{identifier,[{line,18},{column,4}],unless},
              {list,[{line,18},{column,11}],
                    [{identifier,[{line,18},{column,12}],'=='},
                     {identifier,[{line,18},{column,15}],n},
                     {number,[{line,18},{column,17}],0}]},
              {list,[{line,19},{column,5}],
                    [{dot,[{line,19},{column,8}],{io,format}},
                     {binary_string,[{line,19},{column,16}],&lt;&lt;"1. n: ~p~n"&gt;&gt;},
                     {literal_list,[{line,19},{column,29}],
                                   [{identifier,[{line,19},{column,30}],n}]}]},
              {list,[{line,20},{column,5}],
                    [{dot,[{line,20},{column,8}],{io,format}},
                     {binary_string,[{line,20},{column,16}],
                                    &lt;&lt;"2. hello macro~n"&gt;&gt;}]}]}]}
</code></p>

<p>其中可以看到有<code>{list, _, [{identifier, _, unless}, ...]}</code>这样一个宏调用。因为这个宏是在同一个模块的前面定义的，Kapok 编译器就将<code>f</code>之前的代码，包括<code>g</code>, <code>unless</code>的定义作为另一个模块编译完后，用<code>f</code>中调用<code>unless</code>的实际参数 AST 作为参数来调用<code>unless</code>。即<code>unless</code>被调用时得到的参数如下：</p>

<p><code>
{list,[{line,18},{column,11}],
      [{identifier,[{line,18},{column,12}],'=='},
       {identifier,[{line,18},{column,15}],n},
       {number,[{line,18},{column,17}],0}]},
[{list,[{line,19},{column,5}],
       [{dot,[{line,19},{column,8}],{io,format}},
        {binary_string,[{line,19},{column,16}],&lt;&lt;"1. n: ~p~n"&gt;&gt;},
        {literal_list,[{line,19},{column,29}],
                      [{identifier,[{line,19},{column,30}],n}]}]},
 {list,[{line,20},{column,5}],
       [{dot,[{line,20},{column,8}],{io,format}},
        {binary_string,[{line,20},{column,16}],&lt;&lt;"2. hello macro~n"&gt;&gt;}]}]
</code></p>

<p>其中第一个参数是一个 list AST，第二个参数是一个 List，它的元素也是两个 AST。这一串 AST 流经<code>unless</code>函数之后，经过转换返回的结果是</p>

<p><code>
{list,
   [{line,10},{column,4}],
   [{identifier,[{line,10},{column,5}],'case'},
    {list,
        [{line,10},{column,10}],
        [{dot,[{line,10},{column,15}],{core,'true?'}},
         {list,
             [{line,18},{column,11}],
             [{identifier,[{line,18},{column,12}],'=='},
              {identifier,[{line,18},{column,15}],n},
              {number,[{line,18},{column,17}],0}]}]},
    {list,
        [{line,11},{column,6}],
        [{atom,[{line,11},{column,7}],true},
         {atom,[{line,11},{column,12}],ok}]},
    {list,
        [{line,12},{column,6}],
        [{atom,[{line,12},{column,7}],false},
         {list,
             [{line,13},{column,7}],
             [{dot,[{line,13},{column,10}],{io,format}},
              {binary_string,
                  [{line,13},{column,18}],
                  &lt;&lt;"before exprs in body!~n"&gt;&gt;}]},
         {list,
             [{line,19},{column,5}],
             [{dot,[{line,19},{column,8}],{io,format}},
              {binary_string,[{line,19},{column,16}],&lt;&lt;"1. n: ~p~n"&gt;&gt;},
              {literal_list,
                  [{line,19},{column,29}],
                  [{identifier,[{line,19},{column,30}],n}]}]},
         {list,
             [{line,20},{column,5}],
             [{dot,[{line,20},{column,8}],{io,format}},
              {binary_string,
                  [{line,20},{column,16}],
                  &lt;&lt;"2. hello macro~n"&gt;&gt;}]}]}]}
</code></p>

<p>从中可以看到输入的 AST 参数已经被转换插入到指定的地方，这个<code>unless</code>返回的 AST 也将被插入到<code>f</code>中<code>unless</code>被调用的地方，从而达到修改/变换代码的效果。由于篇幅所限，<code>f</code>以及整个模块最后生成的 Erlang Abstract code 就不一一展示了。最后贴出整个示例的完整模块 Kapok 代码如下：</p>

<p>```
(ns x
  (require io))</p>

<p>(defn g [n]
  (io.format “call g(n = ~p)~n”, [n]))</p>

<p>(defmacro unless [test &amp;rest body]
  “"”Evaluates test. If logical false, evaluates body in an implicit do.”””
  (io.format “call unless macro~n”)
  `(case (core.true? ~test)
     (^true ^ok)
     (^false
      (io.format “before exprs in body!~n”)
      ~@body)))</p>

<p>(defn f [n]
  (g n)
  (unless (== n 0)
    (io.format “1. n: ~p~n” [n])
    (io.format “2. hello macro~n”)))</p>

<p>(defn main []
  (f 0)
  (f 1))
```</p>

<p>假定这份代码在文件<code>x.kpk</code>中，编译安装 kapok 之后(具体的编译安装过程可以参考 Kapok 文档，不再详述)使用命令</p>

<p><code>
$ kapok x.kpk
</code></p>

<p>即完成编译<code>x</code>模块，并运行其中的<code>main</code>函数。可得到结果输出</p>

<p><code>
call unless macro
call g(n = 0)
call g(n = 1)
before exprs in body!
1. n: 1
2. hello macro
</code></p>

<p>在代码里，<code>main</code>函数调用了 2 次<code>f</code>，<code>f</code>又调用了<code>g</code>，于是有 2 行<code>call g(...</code>的输出。然而由于<code>unless</code>是在编译时被调用的，只在<code>f</code>的定义里被调用了一次，所以只能看到 1 行”call unless macro”的输出，而且<code>unless</code>展开时输出的这一行在最前面，因为它是在编译阶段执行的，而所有其它<code>io.format</code>都是在运行阶段执行的。最后我们来看一下最后三行输出，”before exprs …“一行是在 Macro 里面定义的，”1. …”, “2. …“这两行则是在调用<code>unless</code>时的输入参数，它们按指定的顺序被组合到同一个<code>case</code>分支中。</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[念念不忘，亦有回响]]></title>
    <link href="http://hhkbp2.github.com/blog/2016/06/08/minds-echo/"/>
    <updated>2016-06-08T00:00:00+08:00</updated>
    <id>http://hhkbp2.github.com/blog/2016/06/08/minds-echo</id>
    <content type="html"><![CDATA[<p><img src="/images/uploads/lights_in_the_grandmaster.jpg" title="“” “电影《一代宗师》佛前灯”" ></p>

<p>多年以前，当我在 Linux 下选择 Emacs 做为编辑器的时候，似乎只是一个比较随机的选择。当时我已经接触并使用 VI 有一段日子，然而我使用它的体验并不算愉快，也许是我被 VI 命令模式和编辑模式搞乱了太多次，于是想找个其它编辑器试试，而当时我所知道的 Linux 终端字符界面编辑器并不多，Emacs 是其中一个，然后我就开始使用 Emacs，从此一发不可收拾，走上了一条不断修炼的漫漫长路。这段经历后来浓缩成了<a href="/blog/2013/01/08/the-pragmatic-emacser/">《Emacs 修炼之道》</a>这篇文章。Emacs 不仅给我提供了一个舒适高效的编辑器工具与环境，也引领我走入了 Lisp 语言的世界。</p>

<p>Lisp 语言诞生自几十年前，这几十年间世事沧桑，一个又一个浪潮在计算机行业冒升又落下，一门又一门新的编程语言流行而又衰落，Lisp 语言早已退出流行的行列，然而却始终吸引着一小群程序员前仆后继地不断进入它的世界。除了传统的几种 Lisp 方言，2007 年出现的 Clojure 将 Lisp 在 JVM 平台重新做了一个现代的实现，也掀起了一波新的潮流，吸引了很多人的关注和使用。几十年前诞生的编程语言，大多早已消失在历史的长河中了，为什么 Lisp 能够保持长期的生命力呢？正如看待黑客文化时有人主要关注它的政治正确，有人则将它看成是嗡嗡作响的经济引擎，不同的人对 Lisp 优点的看法也大不相同。有人喜欢 Lisp 以 List 作为唯一代码组织方式的简单明了，有人欣赏 Lisp 由核心几个特殊 Form 支撑起来的体系，对应着数学领域里面的公理上搭建大厦的体系结构，有人则更实利化只关注可以帮助提高开发效率的那些方面。在我看来，Lisp 最吸引我的一点就是强大的元编程能力，非常便于编写 DSL。当然这并不是说其它方面不重要，如交互式编程，强调函数式风格但又不强制函数式为唯一风格等特点也是可圈可点。</p>

<p>Lisp 的思想和设计是如此优秀，然而传统的几种方言的实现 Scheme，Common Lisp 还有另外几个方言都没能紧跟这个时代的步伐，Clojure 的作者 Rich Hickey 必定是深知这点，才会创造了 Clojure。感谢 Rich，我们得以知道在某个语言的成熟 VM 上实现一个现代 Lisp 的可行性，也欣赏到了 Clojure 设计与哲学的种种优雅美丽之处。Clojure 设计于运行在 JVM 之上，固然是它的成功关键，因为如此一来就可以重用成熟的 JVM 环境以及丰富的代码库，同时 Java 程序员这个群体也非常庞大，能吸引到的 Clojure 使用者也多。另一方面也产生了一些缺点，因为底层的 JVM 原本是为 Java 设计实现的，而 Java 的并发模型还停留在操作系统进程/线程和锁这一种方式，因此 Clojure 除了保留 Java 的并发原语之外，还在并发上做了多种尝试，像软事务内存，几种不同的原语 delay, future, promise，还有几种不同的引用类型。如此多的新概念和原语不可谓不丰富，同时也是繁多复杂。每当我的脑细胞因为折腾可变与不变或者 write skew 而被烧死一部分的时候，我总是怀念 Erlang Actor 并发模型的简单。</p>

<p>既然要顺适多核化的趋势，就需要语言本身的并发机制支持用户态进程/协程。那么为什么不直接使用 Erlang 呢？虽然 Erlang 并不是 Lisp，但 Erlang 的设计也受到 Lisp 的影响，很多地方都有 Lisp 的影子，比如交互 shell，热更新等。然而我对 Erlang 也有觉得不满意的地方，除了经常被, ; .几个符号搞晕之外，最大的不满是来自于在 Erlang 里做元编程的不便，但是 Erlang 代码又有很强烈的元编程需求，比如 OTP 里面的大片模板代码。因此在编写 Erlang 代码的时候，我又相当怀念 Lisp 的 Macro。</p>

<p>那么，能否把 Erlang 和 Lisp 两者的优点结合起来呢，就像在 JVM 上实现 Clojure 一样，也许可以在 Erlang VM 上实现一门 Lisp 语言？到我的脑海中冒出这个想法的时候，距离我最早接触 Erlang 已经有三年多时间，距离学习 Clojure 已经有两年，另外当时在工作上我也写了大半年的 Golang，是的，近几年中我学习/使用过的编程语言也不少，基本上每年都会学一到两门新语言，虽然不是每个新学语言都有机会用来写大量的代码，但也算是见识了不少编程语言，也许是对很多好的语言有着更高的期望吧，没有哪种已知的编程语言令我觉得非常满意。带着把 Erlang 和 Lisp 结合的想法，我发现原来 Erlang VM 就像 Java VM，VM 之上也有中间语言层，可以基于这中间层来实现一门 Lisp，而且，早在几年前，就已经有人这样做了。我发现了 Robert Virding 写的 LFE，和 Eric Merritt 写的 Joxa。</p>

<p>作为参与设计 Erlang 的元老，Robert 一直以来也是探索 Erlang 应用方面的先行者，早在 2007 年就写了 LFE，并且影响了一众后来基于 Erlang VM 的新语言，如 Elixir。LFE 是 Lisp Flavored Erlang 的缩写，顾名思义，LFE 就是把 Erlang 写成 Lisp，除了多了一些括号之外(去掉了 Erlang 原来的分隔符如, ; .等)跟 Erlang 十分相似，LFE 的设计目标在于提供一个可扩展语法的 Erlang，它的实现也达到这个目标。然后跟我理想中的 Lisp 语言相去有一定距离，我并不喜欢它的 Lisp-2 风格，跟 Erlang 相似的模块设计如显式的 export，只在编译时可用的 Macro 等方面。</p>

<p>另外一个实现 Joxa 则是由 Eric 在 2011 年开始写的，Eric 也是 Erlang 界的老前辈了，他先是试用过 LFE，然后觉得并不如意才重新设计和实现了 Joxa，之前我写过的文章<a href="/blog/2015/08/20/joxa-a-lisp-programming-language-base-on-erlang-vm/">《Joxa: 一种基于 Erlang VM 的现代 Lisp 编程语言》</a>详细描述了两者的渊源和差别，这里就不再展开了。总的来说，Joxa 的 Lisp 味相对更浓一些，更符合我的个人口味。在过去的一年多时间里，我仔细阅读了 Joxa 的实现并添加了 Erlang R17 后引入的 Map 的语法。Joxa 虽好，但有两个主要原因让我不得不考虑重新做一个实现：</p>

<ol>
  <li>
    <p>Joxa 目前的实现是用自举的方式将 Lisp 代码经过编译转换成 Core Erlang，自举是很 cool 的一种实现编译器的方式，然而用在语法不够稳定的场景下，会提高后续语言开发的复杂度和难度。Core Erlang 也是一个规模小巧设计良好的中间层，然而文档方面相当缺乏，很多时候必需花大量时间人手去获取从上层到它的翻译细节。相对来说更上层的 Abstract Format 虽然规模更大，然而文档较多，一些工具也只工作在这一层，所以更适合用于做为编译器的目标语言。在这一点上我和 Eric 讨论得出的一致结论是，要将 Joxa 用 Erlang 重写并将目标输出定位到 Abstract Format，却因为彼此都忙暂时未有足够精力将其重写。</p>
  </li>
  <li>
    <p>Joxa 的设计目标强调它要小而简，希望成为一个小的核心语言，如果其他人有需要就在其上定制扩展。这种设计也是 Lisp 语言的一种传统特色，由于扩展性强，可以做到核心小扩展大，类似的思想也被人在不同的语言和项目上实现，比如 PyPy 和 Python 3。然而我更希望有一套功能较多较全的编程语言，因此需要在语法和代码规模上都需要添加(或者改动，但主要是添加)很多内容，这就与 Joxa 的目标有冲突。据我所知 Eric 对 Joxa 有明确的定位，应该不会乐意在 Joxa 里面接受这些添加或改变，因此最好我还是用一个新项目名字来重写一个基于 Erlang VM 的 Lisp 实现。</p>
  </li>
</ol>

<p>一直以来我有个朦胧的想法，希望在下一个我能够自己决定命名的项目，能够用上一个带有我居住所在地中国华南地区的风物，或者带有东南亚色彩的名字，在给这个新项目取名时，老家旧居楼房前面的两棵高大的木棉树出现在我的脑海，那是一种在南方非常常见非常普通的树，平时不会特别觉察到它们的存在，然而有时觉察到它们的存在却又不禁觉得特别，像鲁迅笔下所说：“在我的后园，可以看见墙外有两株树，一株是枣树，还有一株也是枣树”。于是就将这个新项目的名字取为木棉，英文是 Kapok。</p>

<p>我想，Kapok 这个新的基于 Erlang VM 上的 Lisp 实现会包含如下这些内容：</p>

<ol>
  <li>
    <p>一套类似 Clojure 风格的现代 Lisp 语法(Lisp-1)，当然在语法上并不能无脑照抄 Clojure，比如方括号[]在 Clojure 中表示 Vector，在 Erlang 却是 List，那么 Kapok 里如何选择？若语义跟随前者，是用 Erlang 的 tuple 来实现 Vector 语义还是另外再做一套实现？若语义跟随后者，和普通圆括号()又如何区分，各自的使用场景如何确定？每处细节都需要仔细考量，这些细节也会影响其它方面，如下面将提到的兼容性。另外 Clojure 语法糖较多，而我不喜欢太甜。</p>
  </li>
  <li>
    <p>与 Erlang VM 保持最大的兼容，在 Erlang VM 上实现的多种语言，都选择了将语法及语义尽量向 Erlang 靠拢，比如新语言的函数直接编译成 Erlang 的函数，如此得以保持良好的兼容性，新语言的代码可以直接与 Erlang VM 基础设施与已有库代码进行相互调用，不像 Clojure 那样需要通过中间包装。Erlang 是一门函数式语言，这意味着新语言最好也保持这种函数式的语义，当然也可以通过上层逻辑修改这种限制，比如另一门基于 Erlang VM 的语言 Elixir 通过在编译器做变量名映射的技巧使得在 Elixir 里同一个变量可以做多次绑定，如此一来 Elixir 在变量使用上更像命令式语言。基于两个原因我个人更偏向于在 Kapok 里面完全保留 Erlang 的函数式语义，一是它利于并发，二是它语义更清晰。</p>
  </li>
  <li>
    <p>新的语言机制，目前已经在考虑中的有：支持 Clojure 的 Protocol(运行时类型分派，让语言更动态)，探索一套带类型标注的 DSL 来做强类型编程(强制编译时类型检查，让语言更静态)，Lazy API。其中第 Protocal 和 Lazy API 源自 Clojure(当然 Clojure 也借鉴了其它语言)，并且已经在 Elixir 中得到实现。同时我对其它新想法持开放的态度。</p>
  </li>
  <li>
    <p>功能丰富接口现代的标准库，包括常用宏，文件，网络等方面，特别一提的是 Erlang 原生用字符列表来表示字符串并不理想，因此需要有一整套高效易用的 unicode 字符串标准库，在 LFE 和 Elixir 中都重新定义了 binary string，这是一个比较好的方案，可以借鉴。</p>
  </li>
  <li>
    <p>强大且现代的开发工具集，包括：文档及 Apropos 接口/工具，编辑器集成(如 Emacs mode), 项目管理工具(参考 Mix)，交互性开发环境(集成 Slime 和 Swank)等</p>
  </li>
</ol>

<p>上面的几点综合了 Clojure, Erlang, Joxa, LFE, Elixir, Emacs 等多种语言或机制，希望可以取多家之长，提供一套强大完整的开发环境。当然也可以说是一个大杂烩，然而不是无脑照搬，库和工具集可以丰富多样，然而基础语法需要保持简约节制，避免出现 C++或 Scala 那样过于烧脑的设计。同时也要了解到上面几点内容只是一个初步的想法，后面可能会根据实际情况有所添加或改变。我一直有个用一门新编程语言编写一个开源数据库的朦胧想法，Kapok 完善到一定程度之后，也许可以用 Kapok 来完成我这个想法。</p>

<p>有了简要设计，就可以开工编写代码了，首先要实现的是 1, 2 两点，亦即核心编译器部分。要写 Kapok 的想法早在去年就已经产生，然而一直拖拖拉拉未能动手。去年年中祖母去世，让我明白到时间匆匆人生太短，有些事情想做就抓紧时间去做，迟早人生的终点只是青山上的一把黄土。于是开始动手，由于平时上下班剩余时间不多，生活琐事缠身，进展较为缓慢，拖拉了几个月逐渐完成了编译器前端，大概到今年初开始写后端，也算是慢慢重温了一遍大学时开的编译课程。最近有了一些时间，终于折腾到可以跑起来的程度。我想 Kapok 这个项目不可谓不大，终究不能短期内完成，要做到非常完善的程度，更是有待时日。就像长跑，并非重在一时之快慢，积跬步恒坚持，方能走得更久更远。我所需要做的，就是调整呼吸，跨开步子。</p>

<p>于是就把 Kapok 开源了，github 地址在<a href="https://github.com/kapok-lang/kapok">这里</a>，虽然暂时还有很多问题和缺点，也有很多想法未能实现，但我想这也算是一个不错的长跑起步。</p>

<p>十年之前，当我第一次安装并启动 Emacs 的时候，决没有料到会十年之后它带领我在 Lisp 的世界走了这么远。几年之前在接触 Erlang，Clojure 的时候，没有想到有一天会用 Erlang 重写一门 Clojure 风格的 Lisp 语言。一年之前开始着手 Kapok 的时候，我也不知道这个项目要做到何时，做到什么程度。然后事情冥冥中就这样发生了，而我将沿着这条路继续向前走。王家卫电影《一代宗师》里面，宫先生说“念念不忘，必有回响”，行知做人跟练武一样，秉着意念坚持下去，终究能有一点回响。</p>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Joxa: 一种基于 Erlang VM 的现代 Lisp 编程语言]]></title>
    <link href="http://hhkbp2.github.com/blog/2015/08/20/joxa-a-lisp-programming-language-base-on-erlang-vm/"/>
    <updated>2015-08-20T20:34:00+08:00</updated>
    <id>http://hhkbp2.github.com/blog/2015/08/20/joxa-a-lisp-programming-language-base-on-erlang-vm</id>
    <content type="html"><![CDATA[<p><a href="http://joxa.org/">Joxa</a> 是一种基于 Erlang VM 的现代 Lisp 编程语言，创始人是美国的 Eric Merritt<a href="#1">[1]</a>。通过在 Erlang VM 上引入一个精心设计的 Lisp 语法，它保留了 Lisp 和 Erlang 两者的众多优点：简洁而且语义清晰的 Lisp 语法，强大的 Macro，鼓励交互式开发，支持高并发，函数式风格等，并且与现有的 Erlang 平台保持良好兼容。它是一门功能全面的通用编程语言。</p>

<p>Joxa 的官方网站是 <a href="http://joxa.org/">joxa.org</a>，在这个官方网站上，有它的源代码 <a href="https://github.com/joxa/joxa">github 地址</a>，以及<a href="http://docs.joxa.org/en/latest/">在线文档</a>。为什么这门编程语言取名为 Joxa 呢？关于 Joxa 这个名字的由来，Eric Merritt 曾经对我在邮件组的提问做过如下解释：</p>

<blockquote>
  <p>其实这个名字并无特别的意义。很多年前我想开始一个“基于 Java 的某个项目”，于是有了它的缩写 Joxa 这个名字。这个项目从来没有开始做过，但我把域名买下来了并一直持有着。到今天，4 个字母的域名已经很少能注册到了，所以我决定用它来做为这门语言的名字。<a href="#2">[2]</a></p>
</blockquote>

<p>除了名字开头有个字母”J”，Joxa 与 Java 并没有太大的关系了，Joxa 主要受到了 Erlang 及基于 JVM 的编程语言 Clojure 的影响。下面我们先介绍一下 Erlang 和 Clojure，再讨论 Joxa 受到了它们的哪些影响。</p>

<h2 id="erlang-clojure--joxa">1. Erlang, Clojure 以及 Joxa</h2>

<h3 id="erlang">1.1 Erlang，高并发的函数式容错编程语言</h3>

<p>Erlang 是一门通用的并发程序设计语言，它由瑞典爱立信的 Joe Armstrong 在上世纪 80 年代开发，并于 1998 年对外开源，Erlang 这个名字来源自丹麦数学家及统计学家 Agner Krarup Erlang。经过近 30 年的发展，Erlang 目前是支持高并发的编程语言翘楚之一，它在语言层面封装了 Actor 模型，实现了用户空间的轻量级进程，将消息传递作为 Actor 间通信的唯一方式，避免了由传统的线程和锁在并发方面的限制与缺点。Erlang 被设计为电信级系统的编程语言，强调分布式，容错，软实时和公平调度，在语法上它主要受到 Prolog 及 Lisp 的影响，保留了函数式，动态，交互式开发等特点。具体来讲，Erlang 这个名字可以分成 3 个方面的要素：</p>

<ol>
  <li>编程语言 Erlang 本身</li>
  <li>总称为 OTP 的一系列程序设计原则及代码库</li>
  <li>称为 Erlang VM 或 BEAM 的 Erlang 虚拟机</li>
</ol>

<p>Erlang 设计于近 30 年前，因此从现在的角度来看，它在语法，编程环境(包括文档等)及工具链等方面有很多地方都有改进空间。由于 Erlang VM 是目前工业界支持高并发的最成熟的 VM 之一，大量技术专家及工程师们在上面投入了无数的工作，通过在 Erlang VM 上设计一门新语言来重用 Erlang VM 的优良特性，既能发挥 Erlang VM 的长处，又能改善 Erlang 语言本身在语法、工具链等方面的缺点，扬长而避短，是比较完美的方案。业界近几年涌现了众多基于 Erlang VM 的编程语言，下面介绍其中几种：</p>

<ol>
  <li>LFE<br />
 <a href="http://lfe.io/">LFE</a> 是 Lisp Flavored Erlang 的缩写，它是由 Robert Virding<a href="#3">[3]</a>于 2007 年开始开发的一门函数式的并发的通用编程语言，LFE 采用了 Lisp-2<a href="#4">[4]</a>风格的语法，通过将 LFE 代码编译为 Core Erlang 代码运行在 BEAM 上，保留了 Erlang VM 分布式，容错，软实时等优点，同时支持 Lisp Macro，使得 LFE 兼有强大的元编程能力，并实现了一个功能丰富的 REPL<a href="#5">[5]</a>。</li>
  <li>Reia<br />
 <a href="http://reia-lang.org/">Reia</a> 是一门基于 Erlang VM 上的类似 Ruby 的脚本编程语言，它由 Tony Arcieri<a href="#6">[6]</a>于 2010 年中开始开发，它在 Erlang VM 的分布式，并发，容错，热更新的基础之上，引入了 Ruby 的友好语法，灵活的代码块，反射及元编程功能力。遗憾的是，Reia 在 2011 年宣布不再更新。</li>
  <li>Elixir<br />
 <a href="http://elixir-lang.org/">Elixir</a> 是一门动态的函数式编程语言，它由 José Valim<a href="#7">[7]</a>于 2012 年开发。Elixir 同样采用了类 Ruby 的语法，通过支持强大的 Macro 功能，它在简洁的语言核心上，建立了一系列的标准库，包括 Unicode 字符串及相关操作，重写了单元测试框架，丰富的数据类型等，它吸收了 Clojure 的 Protocol，严格和惰性 API，还提供了现代的交互命令行，脚本相关的库函数及项目管理工具。通过将 Elixir 代码编译为 Erlang AST，Elixir 得以重用 Erlang VM 的高并发及高效率，克服了 Ruby 在并发方面的缺陷。由于得到 Jose 及其它 Ruby 界牛人的喜爱及宣传<a href="#8">[8]</a>，Elixir 在近两年开始流行起来。</li>
</ol>

<p>由于本文主要是讲 Joxa，上述这几种编程语言只是简单带过，就不详细展开了。</p>

<p>上面几种编程语言的设计方案虽然有很多不同之处，但从整体思路上几乎是相同的，那就是：通过将代码编译成为 Erlang VM 上的代码，良好兼容 Erlang VM，于是保留了上述 3 要素中的后两点，同时从头设计语言的语法，并在标准库，工具链等一些方面做补充完善。Joxa 的设计也是类似，它选择了 Lisp 做为 Erlang VM 上的新语言，不过它走了一条和 LFE 不同的道路。Eric Merritt 后来专门写了一个博客文章<a href="http://blog.ericbmerritt.com/2012/02/21/differences-between-joxa-and-lfe.html">《Differences Between Joxa and LFE》</a>来谈 Joxa 和 LFE 的不同之处，他说：</p>

<blockquote>
  <p>最主要和重要的区别在于这两门语言的目标。我认为 Robert 实现 LFE 的主要目标在于提供一个可变的语法可扩展的 Erlang 版本，如此一来人们就可以在需要时改变语言。同时我坚信 Robert 喜欢实现编程语言，他应该很享受实现 LFE 的过程。我当然也乐于实现 Joxa，然而，当坐下来实现 Joxa 的时候我怀有一些非常特定的目标：  </p>

  <ol>
    <li>我需要一个用于开发 DSL(Domain Specific Language，领域特定语言)的平台  </li>
    <li>我想要一个更具交互性和动态的开发环境。类似于 Slime 和 Swank 那种<a href="#9">[9]</a>  </li>
    <li>我希望充分利用所有已经存在的相当优秀的 Lisp 工具  </li>
  </ol>

  <p>上述每点都可以在 Erlang 里面解决。例如，我可以用 Leex 和 Yecc<a href="#10">[10]</a>实现 DSL，但我实现 DSL 的最好体验总是来自 Lisp：使用 Lisp 函数和 Macro 来打造这些 DSL。不过我使用 Erlang 有很长时间了，我不愿意放弃 Erlang VM 上面的优良功能来换成 Lisp 的种种优点。唯一的解决方法似乎只有使用一门基于 Erlang VM 之上的 Lisp 语言。</p>

  <p>显而易见的首先选择是 LFE，于是我花了几周时间深入研究这门语言和它的内部实现。最后我得到这个结论：它并没有满足我的需求。剩下的唯一退路就是我自己重新创造一门语言(同时也有一点怀疑自己不太明智)。  </p>
</blockquote>

<p>从整体来看，LFE 更像一门披着 Lisp 外衣的 Erlang，相当于给原来 Erlang 语法添加了括号和 Macro，这与 Eric Merritt 理想中的 Erlang VM 上的 Lisp 语言相去甚远，于是他创造了 Joxa，而 Joxa 的语法及风格受到 Clojure 的影响更大。为什么 Clojure 能受到 Eric 的如此青睐呢？它到底有什么出众之处呢？下面我们来了解一下。</p>

<h3 id="clojurejvm--lisp-">1.2 Clojure，JVM 上的函数式 Lisp 编程语言</h3>

<p>Clojure 是一门动态的强类型编程语言，作者是 Rich Hickey。它寄居在 JVM 之上，设计成能够与 JVM/Java 良好互操作，既利用了 JVM 所提供的成熟高效的运行环境，也兼容众多流行的 Java 库与框架，同时它采用了 Lisp 语法和 Macro，非常便于表达 DSL，加上一套函数式的持久数据结构，并提供并发机制及惰性语义，使得简洁优雅语言成为函数式编程，并发编程的良好载体，同时重用了成熟流行的 JVM 平台，使得它便于在现有 Java 程序员中推广并流行，在这一点上区别于以往所有独立开发的函数式语言。此外它也吸收了 Java 中的面向对象思想和 CLOS<a href="#11">[11]</a>，发展出 Protocol 及多重方法。另外，Clojure 自带一系列丰富的标准库，定义了一套项目管理规范，并提供了优秀的项目工具及 REPL，使得它在开发环境，交互式开发方面成为佼佼者。</p>

<p>Clojure 设计成为 Java 的一个库包，Clojure 代码会编译成 JVM byte code，正因为它以一种非侵入性的方式运行在 JVM 之上，所以在函数式的语言层面，会有一些其它函数式语言不可能出现的“瑕疵”，例如函数没有尾递归优化。兼容 JVM 平台的已有代码，在重用/连接已有项目方面既是一种优势，但有时混合函数式与命令式代码也会产生实际冲突。在并发方面，语言提供的多种并发原语，delay, future, promise，agent，STM 等虽然强大，但从语言整体来看比较复杂。Clojure 的很多地方可以体会到作者有意保持简单与功能(复杂)的平衡，在设计上做了务实折衷的克制。与此相反，另外一个基于 JVM 的语言 Scala 在设计上就显得博爱放任，看到各个好的特性就收入到语言当中，宛如中国古代的皇帝举国征选妃嫔。</p>

<p>相比各种“主流”编程语言，Clojure 至今仍是小众语言，虽然如此，它的推出仍然不可谓不成功，既培养了一个健康壮大的社区，也在市场上占有一定的流行度，产生了一批具有相当影响力的项目，如流式数据处理框架 <a href="https://storm.apache.org/">Storm</a> 等。Clojure 成功地向人们展示了这几个可能性：</p>

<ol>
  <li>在 JVM 平台实现一个函数式，并发的动态编程语言</li>
  <li>通过融合持久数据结构，Protocol 等优异特性，复兴 Lisp</li>
  <li>如何语言设计上在功能、简单与务实之间取得折衷平衡并树立起自身的特色</li>
</ol>

<p>正因为有些如此之多的优点，Clojure 才对程序员们有着如此之大的吸引力。也难怪身为老 Lisp 爱好者的 Eric Merritt 在创造 Joxa 时会受到 Clojure 的较大的影响。下面我们来谈谈 Joxa 的设计。</p>

<h3 id="joxa-erlang-vm--lisp-">1.3 Joxa, Erlang VM 上的新 Lisp 编程语言</h3>

<p>对照上述多种语言的实现，Joxa 的设计主要有如下几个要点：</p>

<ol>
  <li>上层语言为 Lisp，主要目标为用于写 DSL(或者作为其它上层 Lisp 的元语言)，语言的核心部分要简洁</li>
  <li>底层将 Joxa 代码编译成 Erlang VM 代码，将 Joxa 代码映射到 Erlang 上的对应语法结构，比如 Joxa 里面的函数即为 Erlang 函数</li>
  <li>语言核心之外提供 REPL，方便编译/执行脚本的命令行工具等</li>
</ol>

<p>其中 Lisp 的语法可以参考简洁，优雅的 Clojure，由于 Erlang VM 与 JVM 有着非常多的差异，正如 Erlang 语言与 Java 语言有着非常多的差异，所以可以预期的是，Joxa 在语法上面不能完全保持与 Clojure 一致，同时这里面有一个目标用户的问题：Joxa 更多的是为了 Clojure 程序员转向 Erlang 平台而设计，还是为了 Erlang 程序员转向 Lisp 而设计。若为前者，就尽量保留可能多的 Clojure 语法及规范，若为后者则将语法尽可能向 Erlang 靠拢比较理想。这时 Joxa 选择了后一种，即认为 Joxa 主要是解决 Erlang 现有的问题，所以从语法上来考虑，最后出来的结果很可能是一种 Lisp 与 Erlang 的独一无二的新结合。所有的 Lisp 语言从结构上来看，都具有一种类似数学的体系结构，包括以下几个部分：</p>

<ol>
  <li>一切表达式皆为 List，List 有两种，原子及函数调用。代码即数据(总结为同像性)</li>
  <li>7 个基本原语(又称之为特殊 Form)加上可以操纵语言本身的 Macro，两者作为核心，在此之上演化出整个语言</li>
</ol>

<p>这就像数学体系，最核心的部分是几条基本原理，然后通过逻辑推导，演化出其它数学分支以构成整个体系，可以不断向外扩展。Joxa 将会有同样的结构，核心部分将保持尽可能的简洁，只包括基本原语及 Macro，极简的核心既节省开发成本，也给外延留下尽可能大的空间。此处的外延包括针对特定问题领域而言的 DSL，也包括其它上层的 Lisp 语言，从本质上来说这两者本来就没有区别，只不过因为针对的范围有大有小所以说法不同。从语法设计上，Joxa 会跟 LFE 有如下的不同：</p>

<ol>
  <li>Joxa 会是 Lisp-1，而 LFE 是 Lisp-2</li>
  <li>Joxa 的语法会向 Lisp 靠拢，而 LFE 更像 Erlang</li>
  <li>Joxa 中 Macro 求值语义与 Lisp 更为一致，而 LFE 的 Macro 求值语义与函数求值语言不同</li>
</ol>

<p>为了保持与 Erlang VM 现有的平台等保持无缝兼容，以充分利用现有的 Erlang VM 的开发规范与代码库等，第 2 点是必需的。将 Joxa 建立在 Erlang VM 平台的生态环境之上，固然是因为作者对 Erlang VM 的熟悉与喜欢，客观上也可以充分发挥 Erlang 生态的优势。从上面的叙述也可以看到，众多 Erlang VM 上的非 Erlang 编程语言也采用了这种“无缝兼容”设计，虽然它们在实现层面会有一些不同之处。这一点同时确定了 Joxa 将会保留 Erlang 的一些语言特性，例如按文件划分的模块化，函数式风格，代码要求先通过编译等。</p>

<p>第 3 点与开发环境相关，REPL 是各种 Lisp 方言已经是司空见惯了，Erlang 在设计的时候也吸收了这个概念，但是实现得不如 Lisp 的 REPL 那么好用，比如强制输入为表达式(每行的后面必须输入”.”号)，Record 不能用等。Joxa 的 REPL 会参考 Clojure 与 Erlang 的 REPL，结合前者的完整性和后者的功能，在易用性给予特别的关注。同时针对编译、脚本化等开发流程中的各个阶段都提供编辑器、命令行工具等支持。</p>

<h2 id="section">2. 设计与实现细节</h2>

<p>下面我们来详细讨论 Joxa 的设计与实现。根据上述的设计要点，要将 Joxa 代码要编译成 Erlang VM 代码，必需先熟悉 Erlang 代码的编译过程，在此过程中找出合适的切入点。</p>

<h3 id="erlang-">2.1 Erlang 编译过程</h3>

<p>一个经典的编译过程可以分为如下图所示的多个阶段：</p>

<p><a href="/images/uploads/classic_compilation.png"><img src="/images/uploads/classic_compilation.png" alt="传统编译过程的各阶段" /></a><br />
图 1 经典编译过程的各阶段<a href="#12">[12]</a></p>

<p>经典的编译过程可以分为词法分析，语法分析，语义分析，中间代码生成，中间代码优化，机器码生成等多个阶段。Erlang 的代码编译过程跟经典的编译过程基本一致，也可以分成类似的多个阶段，各个阶段的输入输出如下图所示：</p>

<p><a href="/images/uploads/erlang_compilation.png"><img src="/images/uploads/erlang_compilation.png" alt="Erlang 编译过程的各阶段" /></a><br />
图 2 Erlang 编译过程各阶段的输入输出</p>

<p>其中 Core Erlang 为于 Erlang 代码与 VM 内部中间代码之间的一层，它是在 1999 年前后提出的一种 BEAM(Erlang VM 的最新实现)上的语言，它被设计为：</p>

<ol>
  <li>语法清晰简单，严格的更高阶函数式语言</li>
  <li>尽可能规范化，以便相关代码遍历工具的开发</li>
  <li>从 Erlang 代码向 Core Erlang 代码的翻译应该直白，从 Core Erlang 向 VM 内部实现中间代码的翻译也应该简单</li>
  <li>有良好定义的文本表示形式，语法简单无歧义，便于人阅读，调试及测试</li>
</ol>

<p>由于 Core Erlang 是清晰简单，有良好定义的文本语言，便于作为目标语言，而且 Erlang 的代码优化和错误检测大多都在 Core Erlang 层进行，如果我们要在 Erlang VM 打造新编程语言，那么将新语言的代码编译成 Core Erlang(或 AST)，将会是一个很好的解决方案。很多 Erlang VM 上的语言都选择了这种方案，比如 LFE，但也有语言选择了编译成 Erlang AST，比如 Elixir，精通 Elixir Macro 的人对 Erlang AST 应该比较熟悉。相对于 Core Erlang，Erlang AST 更接近于 Erlang 本身，层次也更高。Core Erlang 相关的功能定义在<code>cerl.erl</code>这个模块里面，包括对如模块、函数等各种 Erlang 语言结构的初始化、操纵等功能的一系列函数。</p>

<h3 id="section-1">2.2  一个简单例子</h3>

<p>下面举一个简单的 Hello World 程序作为例子<a href="#13">[13]</a>，让读者对 Erlang AST 与 Core Erlang 有一个感性认识。原始的 Erlang 代码如下：</p>

<p><code>erlang
-module(test).
-export([hello_world/0]).
hello_world() -&gt;
    io:format("Hello World").
</code></p>

<p>对应的 Core Erlang 代码如下所示：</p>

<p><code>erlang
module 'test' ['hello_world'/0]
    attributes []
'hello_world'/0 =
    fun () -&gt;
        call 'io':'format'
            (“Hello World”)
</code></p>

<p>对比两份代码，可以看到 Core Erlang 与 Erlang 之间的映射还是很直观的。将原始的 Erlang 代码编译为 Erlang AST，可以得到：</p>

<p><code>erlang
[{attribute,1,module,test},
 {attribute,2,export,[{hello_world,0}]},
 {function,2,hello_world,0,
   [{clause,2,[],[],
     [{call,3,
       {remote,3,{atom,3,io},{atom,3,format}},
         [{string,3,"Hello World"}]}]}]}]
</code></p>

<p>编译为 Core Erlang AST 即得到：</p>

<p><code>erlang
{c_module,[],
  {c_literal,[],test},
    [{c_var,[],{hello_world,0}}],
      [],
     [{ {c_var,[],{hello_world,0}},
       {c_fun,[2,{file,[]}],
         [],
         {c_call,[3,{file,[]}],
           {c_literal,[3,{file,[]}],io},
           {c_literal,[3,{file,[]}],format},
           [{c_literal,[3,{file,[]}],
              "Hello World"}]}}}]}
</code></p>

<p>对比两者，容易看出 Erlang AST 更高层更抽象，Core Erlang AST 更底层更规范。</p>

<h3 id="section-2">2.3 编译器</h3>

<p>下面我们继续来讨论 Joxa 的编译过程，由于 Core Erlang(及 AST)可以由 Erlang 编译器编译成最终的机器码，我们只需将 Joxa 代码编译成 Core Erlang AST 便可实现将 Joxa 编译成机器码整个编译过程，从编译领域的分类来看，目标生成的代码是 Core Erlang AST，操纵 Core Erlang AST 可以直接调用<code>cerl.erl</code>的接口函数，因此编译器后端这一块相对是比较简单的，重点在于前端部分：即将 Joxa 代码编译成 Core Erlang AST。由于 Joxa 是 Lisp 语法，Lisp 代码以括号划分边界的代码树的方式来表示，本身就已经有良好的结构，所以前端部分也比较简单。区别于 LFE 或 Elixir 用 Leex 或 Yecc 来生成 LALR<a href="#14">[14]</a>式 Lexer 与 Parser，Joxa 采用了手写 PEG<a href="#15">[15]</a> Lexer 和 Parser 的方式。PEG 编译器的代码量较小，Joxa 编译器是在 Erlang PEG 生成器 <a href="https://github.com/seancribbs/neotoma">Neotoma</a> 生成代码的基础上写成的。(本节涉及到很多编译领域的术语或技术，由于本文主要是介绍 Joxa，篇幅所限故不会详细解释这些术语或技术，有兴趣的读者可以自行寻找相关的资料做进一步了解)</p>

<p>特别值得一提的是，Joxa 的编译过程是自举的，即 Joxa 编译器本身是由 Joxa 代码编写的，这与 LFE 或 Elixir 的编译器用 Erlang 编写不同。Joxa 的自举要求先有一份以 Core Erlang AST 格式存在的具有正常编译功能的代码，这部分代码在 Joxa 的 Github 代码库中，相对根目录的路径是<code>src/ast</code>(后面给出所有的代码路径都相对于根目录)。通过用 Erlang 编译器将这份 AST 代码编译成 BEAM 代码，然后就得到一个能直接在 BEAM 上执行的 Joxa 编译器，然后就可以运行此编译器，将编译器的 Joxa 源代码编译成 Core Erlang AST 格式。整个流程和依赖如下图所示：</p>

<p><a href="/images/uploads/joxa_self_bootstrap.png"><img src="/images/uploads/joxa_self_bootstrap.png" alt="Joxa 的自举及编译流程" /></a><br />
图 3 Joxa 的自举及编译流程</p>

<p>由上图可以看到整个流程三个步骤是一个循环，要成功实现自举，必然要先实现其中的一个部分，在此基础上才能实现其中其它两个部分。在 Joxa 的编译器实现中，AST 这部分先由作者 Eric 人手先写出基本的语法解析功能，然后再编写对应功能的编译器的 Joxa 代码，用 AST 编译出来可执行的编译器，去验证对应的 Joxa 代码，然后再按此流程不断添加更多的功能，错误一般出现在编译 Joxa 代码的时候，此时遇到的错误是由新添加的 AST 代码还是 Joxa 代码引起的，有时并不容易定位出来。虽然 Core Erlang 简单清晰，但手写 Core Erlang AST 是相当繁琐的，而且由于 Joxa 语法本身还在不断演变，从头开发一个这样的自举编译器，其难度可以猜想是比较大的。我曾经为 Joxa 添加过 Map 语法的支持，对此开发流程的复杂性有较深的体会。编程语言的自举也可以按另外一个思路来做：先用另外一门常见的语言，比如 C 语言来写编译器，然后当语言的语法发展到比较稳定成熟的时候，再使用这门语言的本身来实现自身的编译器，由于已经有了一个能够工作经过充分检验的 C 编译器，所以自举的实现就有了一个可靠的保障，大大降低其难度。</p>

<p>编译器这部分的 Joxa 代码的路径是<code>src/joxa-cmp-*.jxa</code>(其中<code>*</code>符号表示通配)。按照编译器前端和后端的分类法，下面我们讨论一下各主要文件的代码分布。PEG 的词法分析部分需要构造一系列对应于词素(lexeme)的正则表达式，首先需要有正则表达式的元操作的函数定义，所谓“元操作”，用各种编程语言里面的正则表达式的术语来说，即是元字符，比如”*“符号用于“匹配 0 个或多个”。在 PEG 里面元操作是通过函数来表达的，这部分的代码在<code>src/joxa-cmp-peg.jxa</code>。词素，比如注释或数字，它们的定义放在<code>src/joxa-cmp-lexer.jxa</code>。<code>src/joxa-cmp-parser.jxa</code>则包含 Parser 的代码。编译器的主要逻辑放在<code>src/joxa-compiler.jxa</code>，它调用 Parser 来解析读入的字符流，成功解析之后调用<code>make-forms</code>函数递归遍历解析得到的语法树来生成 Core Erlang AST，在编译过程中会执行对函数调用的合法性检查，Macro 的递归展开等动作。后端的代码按语义的分类分成下述多个文件：</p>

<p><code>text
├── joxa-cmp-binary.jxa           # Binary
├── joxa-cmp-call.jxa             # 函数调用
├── joxa-cmp-case.jxa             # case 语句
├── joxa-cmp-defs.jxa             # 函数、宏定义
├── joxa-cmp-expr.jxa             # 表达式
├── joxa-cmp-joxa-info.jxa        # 模块 info
├── joxa-cmp-literal.jxa          # 常量，常量表达式
├── joxa-cmp-ns.jxa               # namespace
└── joxa-cmp-spec.jxa             # spec
</code></p>

<p>以上即为 Joxa 编译器实现各部分代码的所在文件。整个编译器实现从代码量上来说并不大。</p>

<h3 id="section-3">2.4 数据类型</h3>

<p>与 Elixir 在 Erlang 数据类型的基础上添加了 Range、正则表达式、Unicode 字符串等新数据类型不同，Joxa 支持的数据类型与 Erlang 保持一致，并没有添加新的数据类型，所有的数据类型包括如下几种<a href="#16">[16]</a>：</p>

<ol>
  <li>简单类型：不定长整数，浮点数，原子</li>
  <li>系统类型：PID, Port, Reference</li>
  <li>集合类型: Tuple, Record，Map, List, Binary</li>
</ol>

<p>各种数据类型的字面量语法请参考 Joxa 的在线文档。值得一提的是，围绕 Record 的各种操作，Joxa 在语法上做了包装，便于解耦 Record 的内部实现与接口，提高了可用性，Elixir 在这个方面走得更远，引入了 Clojure 的 Protocol。另外一个常用的集合类型 set 是通过 Erlang 库提供的，并没有赋予特别的语法。</p>

<h3 id="form-">2.5 特殊 Form 及标准库基础原语</h3>

<p>Joxa 里面的特殊 Form 及标准库中的基础原语包括以下几个:</p>

<ul>
  <li><code>let*</code>, <code>let</code><br />
  用于绑定变量，不同于 Erlang 中的绑定操作或 Clojure 的<code>let</code>操作，<code>let*</code>并不支持 Pattern Matching 或解构，Pattern Matching 或解构需要通过<code>case</code>，标准库中的<code>let</code>是一个用<code>let*</code>和<code>case</code>实现的对应支持 Pattern Matching 的版本</li>
  <li><code>case</code><br />
  整个 Joxa 语言中为数很少的一个支持 Pattern Matching 的原语之一，与 Erlang 里面在函数签名，变量匹配，<code>case</code>语句等各种语法结构都可以做 Pattern Matching 不同</li>
  <li><code>receive</code><br />
  接收消息，但没有对应的<code>send</code>原语，这可以通过调用 Erlang 模块或 OTP 库接口实现，支持 Pattern Matching</li>
  <li><code>do</code><br />
  分组表达式成一块，类似于 Lisp 里面的<code>progn</code></li>
  <li><code>apply</code><br />
  以列表函数调用指定函数，类似于 Lisp 的<code>apply</code></li>
  <li><code>fn</code><br />
  构造匿名函数，类似于 Erlang 的<code>fun</code>，或 Lisp 的<code>lambda</code></li>
  <li><code>defn</code>, <code>defn+</code><br />
  定义模块内可见，模块外可见函数</li>
  <li><code>defspec</code><br />
  用于定义前置声明</li>
  <li><code>defmacro</code>, <code>defmacro+</code>, <code>quote</code>, <code>quasiquote</code>, <code>~</code>, <code>~@</code>, <code>gensym</code>, <code>macroexpand-1</code><br />
  Macro 操作，下一节再展开讨论</li>
  <li><code>use</code>, <code>require</code>, <code>as</code><br />
  namespace 相关操作，源自于 Clojure 里面的对应物，要注意的是不同于 Clojure 默认会在所有 namespace 自动导入<code>clojure.core</code>，Joxa 并不会自动导入<code>joxa-core</code></li>
  <li><code>try*</code>, <code>try</code><br />
  两者<code>catch</code>用于异常捕取，用法跟 Erlang 里面的对应物类似，两者的区别在于是否支持 Pattern Matching，类似于<code>let*</code>和<code>let</code></li>
  <li>
    <p>特殊常量，都以函数方式进行调用</p>

    <pre><code>  ($filename)       ;; 当前文件的文件名(连后缀)
  ($namespace)      ;; 当前的 namespace
  ($line-number)    ;; 当前的行号
  ($function-name)  ;; 当前的函数名
</code></pre>
  </li>
  <li>其它如<code>attr</code>，<code>when</code>等原语就不一一列举了。</li>
</ul>

<h3 id="macro">2.6 Macro</h3>

<p>作为 Lisp 类语言的杀手级特性，以及表达 DSL 的终极利器，一直以来 Macro 在 Lisp 类语言中都有着重要的地位。Joxa 中的 Macro 原语与 Common Lisp 或 Clojure 等之前的 Lisp 语言保持了一致，详细列出如下：</p>

<p><code>text
defmacro     -- 定义模块内部 Macro
defmacro+    -- 定义对模块外部可见 Macro
quote        -- 抑制求值
quasiquote   -- 对应 Common Lisp 里面的 back quote，或 Clojure 里面的 syntax quote，部分求值
~            -- unquote，对符号后面元素进行求值
~@           -- unquote-splicing，对符号后面的 List 元素进行求值并展开到当前位置
gensym       -- 动态生成新变量，用于保证 Macro 健康(或称 Macro 卫生)
</code></p>

<p>另外为方便调试，标准库中提供了<code>macroexpand-1</code>函数，用于单次展开 Macro，这个函数也沿袭于传统的 Lisp 语言，但是并没有提供<code>macroexpand</code>(<code>macroexpand-all</code>)。</p>

<p>在 Joxa 里面使用 Macro，跟之前的 Lisp 语言并没有什么不同，以一个标准库<code>joxa-core</code>模块里的代码为例：</p>

<p><code>clojure
(defmacro+ let (args &amp;rest body)
  (let* (process-arg-body
        (fn (arg)
            (case arg
              ([r e]
               `(case ~e
                  (~r ~@body)))
              ((r . (e . rest))
               `(case ~e
                  (~r ~(process-arg-body rest))))
              (detail (erlang/error {:malformed-let-expression detail})))))
    (process-arg-body args)))
</code></p>

<p>Joxa 在语法上直接定义为 Lisp 风格，因此在 Macro 的定义及使用上面，与传统一脉相承并无修改。在这个 Macro 定义里面，除了 Pattern Matching，以及递归调用<code>process-arg-body</code>之外，与传统 Lisp 语言并无不同，熟悉传统 Lisp 的人可以很快就读懂。对比的来看，在 Elixir 这样的非 Lisp 语言中引入 Macro，由于上层语言的语法与 AST 并不一致，所以程序员必需记住/区分上层语言与 AST 两种环境，因此相对较为复杂，比如，在 Elixir Macro 的签名处 Pattern Matching Erlang AST，就在以 Elixir 的语法编写的 Macro 定义中，暴露了底层的 AST 格式。这种复杂性虽然从设计上来说是必需的折衷，但在习惯了 Lisp Macro 的人看来可能不会太喜欢。Joxa 作者 Eric 在 2015 年一次接受《This is not a Monad tutorial》的<a href="https://medium.com/this-is-not-a-monad-tutorial/eric-merritt-erlang-and-distributed-systems-expert-gives-his-views-on-beam-languages-hindley-a09b15f53a2f">采访</a>中就表示过不喜欢 Elixir Macro 的复杂性。</p>

<h3 id="section-4">2.7 标准库概览</h3>

<p>Joxa 的标准库只包括少数几个基本函数以及对 OTP 的简单包装。详细列出如下：</p>

<ul>
  <li><code>joxa-core</code><br />
  基本操作: <code>!=</code>, <code>lte</code>, <code>gte</code>, <code>and</code>, <code>or</code>, <code>+</code>, <code>-</code>, <code>incr</code>, <code>decr</code>, <code>if</code>, <code>when</code>, <code>unless</code>, <code>try</code>, <code>let</code>, <code>define</code></li>
  <li><code>joxa-eunit</code><br />
  eunit 相关函数封装</li>
  <li><code>joxa-lists</code><br />
  list 相关的功能函数： <code>dolist</code>, <code>hd</code>, <code>tl</code>, <code>foldl</code>, <code>map</code>, <code>lists-binding</code>, <code>#</code>, <code>all</code>, <code>any</code></li>
  <li><code>joxa-records</code><br />
  Record 相关函数</li>
  <li><code>joxa-shell</code><br />
  REPL 函数的简单实现</li>
  <li><code>joxa-otp</code>, <code>joxa-otp-gen-server</code>, <code>joxa-otp-supervisor</code>, <code>joxa-otp-application</code><br />
  Erlang OTP 相关接口的封装函数</li>
</ul>

<p>代码规模很小，跟 Elixir 的标准库相比差得很远。其功能比较简陋，称之为标准库也许太大，或者称之为帮助函数更加准确。由于 Joxa 可以直接调用 Erlang 代码，因此功能缺失之处可由其它 Erlang 库补充。</p>

<h3 id="section-5">2.8 开发环境及工具链</h3>

<p>目前 Joxa 只有一个名为<code>joxa</code>的命令行工具，用于编译 Joxa 源代码，启动 REPL，这个工具的功能也比较简陋，跟 Clojure 的 REPL 还有很大的距离。源代码中有一个 Emacs 的 Major Mode 配置文件<code>emacs/joxa-mode.el</code>，可以在用 Emacs 开发 Joxa 时设置缩进，关键字高亮，键绑定等。上面提到的与 Slime 和 Swank 集成，则尚未开发。</p>

<h3 id="section-6">2.9 项目状态</h3>

<p>Joxa 从 2011 年底开始开发，一直到 2013 年初都比较活跃，这之后代码提交量变得相当少，在当前这个时间点(2015 年 8 月)回头看看提交日志，已经有一年多的时间没有任何更新。虽然还未完成原来的设计目标，wiki 上面的计划也有很多开发要做，但是由于作者 Eric 工作上比较忙，而 Joxa 社区实在太弱小，除作者之外并没有其它的人员贡献过大量代码，因此短期之内似乎项目状态不会重新变得活跃。在 Google Group 上有 Joxa 的邮件组，在近一年多时间内也相当少人发言。在应用上，除了作者 Eric 将 Joxa 用于编写他的创业项目之外，目前市面上没有看到其它的应用<a href="#17">[17]</a>。综合来看，Joxa 的项目状态是比较停滞的。</p>

<h2 id="section-7">3. 总结</h2>

<p>Joxa 是一种基于 Erlang VM 的现代 Lisp 语言，有着简洁清晰的 Lisp 语法，支持强大的 Macro，是在 Erlang VM 编写 DSL 的一个很好的载体，它无缝兼容 Erlang VM 平台，是一门功能全面的通用编程语言。它在语言设计及编译器实现方面质量优良，但目前完成度不高，工具链并不完整，市面上也少见应用。作为一门较新的基于 Erlang VM 的编程语言，它有待进一步的发展完善。</p>

<p><em>注：</em><br />
<a id="1">[1]</a> Eric Merritt 是《Erlang and OTP in Action》(中文译本《Erlang/OTP 并发编程实战》)一书的作者之一，Erlware 项目联合创始人，Afiniate 公司的 CTO。<br />
<a id="2">[2]</a> 翻译自邮件原文<br />
It doesn’t actually mean anything. Many years ago it was an acronym for some project I wanted to start ‘Java oriented something or other’. I never made that project but I bought the domain and have kept it these years. Four letter domains are pretty uncommon these days, so I just decided to use it as the name for the language. Thats all.<br />
<a id="3">[3]</a> 作为 Erlang 语言联合创始人以及 Joe Armstrong 的长期亲密战友，Robert Virding 自从当年在爱立信计算机科学实验室开始，长期以来在 Erlang 的设计，标准库，编译器，发展推广等方方面面都做了杰出的贡献。<br />
<a id="4">[4]</a> Lisp-1 和 Lisp-2 的区别在于函数与变量是否共用同一命名空间，一个详细的解释可以参考文章<a href="http://ergoemacs.org/emacs/lisp1_vs_lisp2.html">《What’s Lisp-1, What’s Lisp-2? Bad Jargon or Good Jargon?》</a><br />
<a id="5">[5]</a> REPL 是 Read-Eval-Print Loop 的缩写，最早被用于指代开发 Lisp 程序过程中，交互式命令行不断执行读取程序员的输入代码，对其进行求值并打印出求值结果的循环动作。后来这个概念被 Python，Ruby 及各种交互式命令行工具吸收并推广开来。<br />
<a id="6">[6]</a> Tony Arcieri 是一位美国的软件工程师，他的博客见<a href="http://tonyarcieri.com/">这里</a>。<br />
<a id="7">[7]</a> José Valim 是一位波兰的软件工程师，他最被人熟知的两个身份是 Ruby On Rails 的核心成员以及 Elixir 编程语言的创始人。<br />
<a id="8">[8]</a> 比如 Joe Armstrong 于 2013 年中写了博客文章<a href="http://joearms.github.io/2013/05/31/a-week-with-elixir.html">《A Week with Elixir》</a>盛赞了 Elixir“结合了 Ruby 和 Erlang 的优良特性”，Dave Thomas 于 2014 年发布了新书<a href="https://pragprog.com/book/elixir/programming-elixir">《Programming Elixir》</a>向有其它语言经验的程序员提供了一本系统的教程。<br />
<a id="9">[9]</a> 译者注：<a href="https://common-lisp.net/project/slime/">Slime</a> 是 Superior Lisp Interaction Mode for Emacs 的缩写，它为 Emacs 提供了一整套交互式开发 Common Lisp 的功能集，包括编译，调试，文档查找等等，Slime 是客户端，Swank 是对应的服务器端，它们共同组成了一个强大的程序开发环境。<br />
<a id="10">[10]</a> 译者注：<a href="http://erlang.org/doc/man/leex.html">Leex</a> 和 <a href="http://erlang.org/doc/man/yecc.html">Yecc</a> 是 Erlang 语言的 Lex 和 Yacc 工具集。<br />
<a id="11">[11]</a> CLOS 是 the Common Lisp Object System 的缩写，是指在 Common Lisp 中实现面向对象机制的一系列代码库。<br />
<a id="12">[12]</a> 此图来自编译领域的经典著作《Compilers: Principles, Techniques, &amp; Tools》第三版，中译本《编译原理》。<br />
<a id="13">[13]</a> 这个例子来自 Eric Merritt 2012 年 8 月在 Chicago Erlang User Group 上的技术分享《Joxa: A Full Featured Lisp on the Erlang VM》，录像视频见<a href="https://vimeo.com/49116180">这里</a>.<br />
<a id="14">[14]</a> LALR 是 LookAhead LR 的缩写，LR 中的 L 表示对输入进行从左到右的扫描，R 表示反向构造出一个最右的推导序列。LALR 是流行的自底向上语法分析方法。<br />
<a id="15">[15]</a> PEG 是 Packrat Expression Parsing 的缩写，它是一种相对较新的自顶向下语法分析方法。<br />
<a id="16">[16]</a> 其中 Map 的语法支持由我添加，写此文时未进入主干分支。<br />
<a id="17">[17]</a> 从推广应用的角度来看，在 2011 年中开始开发的 Elixir 在各个基于 Erlang VM 的新编程语言上是走得最前的。  </p>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[嵌套反引用有害]]></title>
    <link href="http://hhkbp2.github.com/blog/2013/03/10/nested-backquotes-considered-harmful/"/>
    <updated>2013-03-10T21:37:00+08:00</updated>
    <id>http://hhkbp2.github.com/blog/2013/03/10/nested-backquotes-considered-harmful</id>
    <content type="html"><![CDATA[<p>本文翻译自<a href="http://bc.tech.coop/blog/041205.html">《Nested Backquotes considered harmful》</a>，只用于学习，请勿用于其它用途。</p>

<hr />

<p>好吧，我想嵌套反引用（nested backquote）不是真的有害，因为你的确可以用它来做一些没有它就很难做到的事情；然而，它又的确会导致非常难以理解的代码（它对于混乱代码比赛<a href="#1">[1]</a>或许是有用的，但非常难以维护）。实际上，这篇文章的标题应该叫做“嵌套反引用令我头痛”，但我一直想写一篇标题叫做“那谁谁有害”的文章，于是，标题就是这个了。;-)</p>

<blockquote>
  <p>注：如果你不知道什么是嵌套反引用，它为什么有用，或者感兴趣于了解更多关于写出或理解这种典型地由嵌套反引用生成的，用于生成代码的代码，那么 Alan Bawden 的杰出论文<a href="www.linearity.org/bawden/ftp/pepm99.ps.gz">《Quasiquotation in Lisp》</a>值得一看（这是 <a href="http://groups.google.com/groups?hl=en&amp;lr=&amp;selm=41adc0bd%240%24276%24edfadb0f%40dread12.news.tele.dk">Jens Axel Søgaard</a> 所推荐的）。Alan 的论文描述了这项技术的历史和概况，而且读起来很有趣。Paul Graham 在他的书《On Lisp》的第 16 章也谈到了嵌套反引用，他说<a href="#2">[2]</a>：<br />
<code>为了定义一个定义宏的宏，我们通常会要用到嵌套的反引用。嵌套反引用的难以理解是出了名的。尽管最终我们会对那些常见的情况了如指掌，但你不能指望随便挑一个反引用表达式，都能看一眼，就能立即说出它可以产生什么。这不能归罪于 Lisp。就像一个复杂的积分，没人能看一眼就得出积分的结果，但是我们不能因为这个就把问题归咎于积分的表示方法。道理是一样的。难点在于问题本身，而非表示问题的方法。</code></p>
</blockquote>

<p>Bruno Haible 最近<a href="groups.google.com/groups?hl=en&amp;lr=&amp;selm=con4h2%24odi%241%40laposte.ilog.fr">在 c.l.l.解释了避免嵌套反引用的两个不同方法</a>。它们主要包含将嵌套反引用转换为简单反引用的做法。这两个方法他概述如下：</p>

<pre><code>1. 在内层使用 LIST, APPEND 等函数代替反引用
2. 使用包含反引用的辅助函数
</code></pre>

<p>Bruno 为每个方法提供了例子。</p>

<p>首先，下面给出的这段代码用了嵌套反引用：</p>

<pre><code>(defmacro once-only (variables &amp;rest body)
  (assert (every #'symbolp variables))
  (let ((temps nil))
    (dotimes (i (length variables)) (push (gensym) temps))
    `(if (every #'side-effect-free? (list ,variables))
       (progn ,body)
       `(let
          (,,@(mapcar #'(lambda (tmp var)
                          ``(,',tmp ,,var))
                      temps variables))
          ,(let ,(mapcar #'(lambda (var tmp) `(,var ',tmp))
                         variables temps)
             ,body)))))
</code></pre>

<p>原来的代码使用方法 1 转换后如下：</p>

<pre><code>(defmacro once-only (variables &amp;rest body)
  (assert (every #'symbolp variables))
  (let ((temps nil))
    (dotimes (i (length variables)) (push (gensym) temps))
    `(if (every #'side-effect-free? (list ,variables))
       (progn ,body)
       (list 'let
         (list ,@(mapcar #'(lambda (tmp var)
                             `(list ',tmp ,var))
                         temps variables))
         (let ,(mapcar #'(lambda (var tmp) `(,var ',tmp))
                       variables temps)
           ,body)))))
</code></pre>

<p>原来的代码使用方法 2 转换后如下：</p>

<pre><code>(defun construct-binding (variable form)
  `(,variable ,form))

(defun construct-let-wrapper (bindings body-form)
  `(let ,bindings ,body-form))

(defmacro once-only (variables &amp;rest body)
  (assert (every #'symbolp variables))
  (let ((temps nil))
    (dotimes (i (length variables)) (push (gensym) temps))
    `(if (every #'side-effect-free? (list ,variables))
       (progn ,body)
       (construct-let-wrapper
         (list ,@(mapcar #'(lambda (tmp var)
                             `(construct-binding ',tmp ,var))
                         temps variables))
         (let ,(mapcar #'(lambda (var tmp) `(,var ',tmp))
                       variables temps)
           ,body)))))
</code></pre>

<p>两个方法中的任一个，产生的代码的可读性都比原来代码好得多。</p>

<p>作为 Bruno 建议方法的一种替代，也许你会考虑使用 <a href="http://www.cs.yale.edu/homes/dvm/">Drew McDermott</a> 的 BQ 反引用工具（包含在 <a href="ftp://ftp.cs.yale.edu/pub/mcdermott/software/ytools.tar.gz">YTools</a> 中）。Drew 在 <a href="http://www.cs.yale.edu/homes/dvm/papers/ytdoc.pdf">YTools 手册</a>中对这个反引用问题有一个很好的解释：</p>

<blockquote>
  <p>反引用是 Lisp 的一个不可或缺的特性。然而在标准说明中它并非尽善尽美。我主要有两个不满：</p>

  <ol>
    <li>
      <p>当实现一个像反引用这样的机制时，需要 3 个东西：一个读取器，一个宏展开器，一个写入器。读取器将如<code>`(foo ,x)</code>的字符序列转换为如<code>(backquote (foo (bq-comma x)))</code>的内部形式（Allegro 就是这样读取的）。之后宏展开器将 backquote 调用转变为像<code>(list 'foo x)</code>构造函数形式。写入器将<code>(backquote (foo (bq-comma x)))</code>输出为<code>`(foo ,x)</code>。</p>

      <p>不幸的是，Common Lisp 标准没有详细说明宏（macro）是什么。因此，它依赖于具体实现。对比来看，普通的引用（quote）有一个定义良好的内部形式<code>(quote x)</code>，因此有一个定义良好的来自外部形式<code>'x</code>的转换。没有详细标准的问题就在于，要写一个你自己的工具配合读取器，宏取开器或写入器，是不可能的。例如，无法写一个可移植的代码遍历程序，针对反引用表达式来做一些特别的事情。事实上，一个 Lisp 实现甚至不要求有一个反引用的内部表示。读取器和宏展开器可以合并，以致于<code>`(foo ,x)</code>可以读作<code>(list 'foo x)</code>。此外反引用写入器的行为并非良好定义的，因为无法区别一个列表构造形式是不是由反引用转化来的。</p>

      <p>为什么要与宏展开器交互呢，这里有个例子。你或许也想和读取器有交互。假设你希望创造一个泛化的反引用读取宏（就叫它<code>!@</code>吧），这个宏建造一些并非列表结构的东西，你可以将<code>(apply #'make-a-foo (list 'baz a) l)</code>简写作<code>!@(make-a-foo (baz ,a) ,@l)</code>。当这个表达式<code>!@(...)</code>被读入时，很多 Lisp 实现会报一个类似“Comma not inside a backquote”（逗号出现在非反引用表达式中）的错，没有可移植的办法来干预读取过程来使它变成合法的。</p>
    </li>
    <li>
      <p>用于阐明嵌套反引用的规则是，逗号是与围绕它的最内层的反引用配对的。（同时提取它的参数到上下文中，以便下一个逗号匹配到下一个反引用，如此类推。）</p>

      <p>我想这是错误的，或者至少在某些情况下是错误的。我从左到右地读反引用代码，因此先看见最外层的反引用。人们会喜欢这样理解：从反引用的角度来看，所有在它里面的东西，除了用逗号标记的之外都是“不会动的（inert）”（即被引用住）。对于所有可能会出现在其中的表达式，除了反引用外，这是成立的。所以如果你正编辑一个复杂的反引用表达式：</p>

      <pre><code>'(foo (bazaroo '(fcn a ,x)))
</code></pre>

      <p>内层的引用不会“遮蔽”住<code>x</code>使它免于被求值。但如果将内层的引用改成反引用，那么遮蔽就正正要发生。你必须把它改成这样：</p>

      <pre><code>'(foo (bazaroo `(,fcn a ,',x)))
</code></pre>

      <p>这个<code>,',</code>构造只是外观丑陋而已。它的唯一作用是将它的参数提取出最内层的反引用；你不能用<code>,,x</code>，因为这是说“当外层的反引用被展开时对<code>x</code>求值，得到<code>e</code>，然后当最内层反引用展开时求值<code>e</code>”。注意求值是次序是从外到内，然而嵌套反引用的规则是从内到外。非常，非常的令人迷惑。</p>
    </li>
  </ol>

  <p>这些不是巨大缺陷：99.9%的反引用不是嵌套的，而且几乎没人关心一个反引用的内部表示是什么。可是如果你感兴趣，文件<code>bq.lisp</code>提供一个可选的实现。  </p>
</blockquote>

<p>因此，正如 Common Lisp 里大多数的事物一样，如果你不喜欢某些东西，你可以改变它！当你需要写这类嵌套反引用的代码时，你有（至少）4 种不同的可以考虑的选择（基于你有多少嵌套反引用工作要做，还有你个人对代码风格／可读性的偏好）：</p>

<pre><code>1. 使用嵌套反引用
2. 在内层使用 LIST, APPEND 等函数代替反引用
3. 使用包含反引用的辅助函数
4. 使用一个反引用的替代实现
</code></pre>

<p>现在，出山吧，去写一些这样的代码：一种形式的代码不断的生成下一种形式，像生物进化的过程那样一代一代衍生。;-)</p>

<p><em>注：</em><br />
<a id="1">[1]</a> 即 Obfuscation Contest。国际 C 语言混乱代码大赛（IOCCC, The International Obfuscated C Code Contest）是一项著名的国际编程赛事。<br />
<a id="2">[2]</a> 此段译文引用田春（伞哥）等人翻译的《On Lisp 中文版》对应章节。</p>

]]></content>
  </entry>
  
</feed>
